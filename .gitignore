# macOS
.DS_Store

# Flutter/Dart (for when the app code lands)
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
lib/l10n/generated/
*.iml
.idea/

# Secrets — the market-data API key is passed via --dart-define, never committed
*.env
