name: tuantuan_stock
description: A cute US-stock price viewer.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  go_router: ^17.3.0
  http: ^1.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  custom_lint: ^0.7.6
  flutter_lints: ^6.0.0
  riverpod_lint: ^2.6.5

flutter:
  generate: true
  uses-material-design: true
