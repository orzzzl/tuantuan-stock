# 08 — i18n (zh + en)

- **Status:** DONE (PR #6)
- **Owner:** Codex
- **Blocked by:** —
- **Note:** do this EARLY — it blocks every UI screen task (12/13/14). AGENTS.md forbids
  hard-coded user-facing strings, so screens must consume ARB from their first PR.
- **Allowed new deps:** flutter_localizations, intl (gen_l10n)

## Goal
ARB-based localization for zh + en so no user-facing string is hard-coded, with the
whole-set language rules from DESIGN.md.

## Scope
- in:
  - gen_l10n setup (`l10n.yaml`, `app_zh.arb` as the template, `app_en.arb`).
  - Migrate every existing string; add lint/check habit: no literals in widgets.
  - **Whole-set swaps, never mixed**: range chips zh `1日/1周/1月/3月/今年/1年` ↔ en
    `1D/1W/1M/3M/YTD/1Y`; session tags zh `盘前/盘后` ↔ en `Pre/Night`.
  - Number/percent formatting via intl (tabular strings already handled by the theme).
- out:
  - No other languages.

## Acceptance criteria
- [ ] Switching device locale swaps all copy, including the chip/tag sets as wholes.
- [ ] No hard-coded user-facing strings outside ARB (spot-check test on key screens).
- [ ] `format`/`analyze`/`test` clean.
